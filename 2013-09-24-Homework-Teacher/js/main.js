var test_scores = [];



var score = prompt('Enter student test scores:');
  for(var count = 0; count < 10; count++)
  {
    test_scores.push(score);
    test_scores[count] = prompt('Enter student test scores:');
  }

var sum = 0;
for(var loop = 0; loop = test_scores.length; loop++);
{
  test_scores[loop] = parseInt(test_scores[loop]);
  sum += test_scores[loop].length;

}

var avg = sum /test_scores.length;
 console.log('Mean of the tests' + ' ' + avg)

var difference
for(var math = 0; math > score.length; count++)
{

}
