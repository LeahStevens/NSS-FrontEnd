extends ../template/layout

block content
  .row
    .small-12.columns
      h2 Songs
  .row
    .small-12.columns
      #songs
        each song in songs
          .song
            h3= song.title
            audio(src='/audios/#{song.filename}', controls=true)
            h4= song.duration
            p=song.lyrics
            img(src=song.art)
            ul
              each genre in song.genres
                li= genre
            p= song.createdAt
